{
  "name": "branch-titlebar",
  "displayName": "Branch Title Bar Color",
  "description": "Automatically colorizes title bar based on git branch",
  "version": "0.0.1",
  "publisher": "local",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "branchTitleBar.updateColor",
        "title": "Branch Title Bar: Update Color"
      }
    ],
    "configuration": {
      "title": "Branch Title Bar Color",
      "properties": {
        "branchTitleBar.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable automatic title bar coloring based on branch"
        },
        "branchTitleBar.saturation": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "description": "Override color saturation (0-100). If not set, automatically generated from branch name (40-60)"
        },
        "branchTitleBar.lightness": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "description": "Override color lightness (0-100). If not set, automatically generated from branch name (28-38)"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^16.x",
    "@types/vscode": "^1.60.0",
    "typescript": "^4.7.4"
  }
}
